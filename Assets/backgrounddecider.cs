using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class backgrounddecider : MonoBehaviour
{
  [SerializeField] Sprite[] sprites = new Sprite[1];
    [SerializeField] string[] mallines = new string[1];
    [SerializeField] string[] benlines = new string[1];
    [SerializeField] string[] myslines = new string[1];

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
